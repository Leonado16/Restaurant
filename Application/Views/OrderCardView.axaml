<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:vm="clr-namespace:Restaurant.ViewModels"
			 x:Class="Restaurant.Views.OrderCardView"
			 x:DataType="vm:OrderCardViewModel">

	<Border Padding="18" Margin="10, 0, 10, 0"  Height="200" VerticalAlignment="Top" Background="Beige">
		<StackPanel>
			<TextBlock Padding="10, 2" Text="{Binding Order.RecipeFollowed.Name}"/>
			<TextBlock Padding="2 2" Text="{Binding Order.CurrentWorkstation}"/>
			<TextBlock Padding="2, 2" Text="{Binding CurrentStepDisplay}"/>
			<ProgressBar Minimum="0" Maximum="100"
						ShowProgressText="True"
						 Value="{Binding Order.OrderProgress}"
						 ProgressTextFormat="{}{1:0}%">
			</ProgressBar>
		</StackPanel>
	</Border>
</UserControl>
